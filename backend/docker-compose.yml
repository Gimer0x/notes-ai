services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: pith
      POSTGRES_PASSWORD: pith
      POSTGRES_DB: pith
    ports:
      - "5432:5432"
    volumes:
      - pith-pg:/var/lib/postgresql/data

volumes:
  pith-pg:

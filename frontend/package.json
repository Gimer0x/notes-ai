{
  "name": "pith-frontend",
  "version": "0.0.1",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build:native": "zsh native/build-helper.sh",
    "build": "npm run build:native && tsc && mkdir -p dist/renderer && cp src/renderer/index.html dist/renderer/",
    "start": "npm run build && electron .",
    "start:dev": "npm start",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "form-data": "^4.0.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^34.0.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.7.0"
  },
  "build": {
    "appId": "dev.pith.capture-spike",
    "productName": "Pith Capture Spike",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "src/renderer/**/*"
    ],
    "extraResources": [
      {
        "from": "native/CaptureHelper.app",
        "to": "CaptureHelper.app"
      }
    ],
    "mac": {
      "target": [
        "dmg",
        "zip"
      ],
      "category": "public.app-category.productivity",
      "extendInfo": {
        "NSMicrophoneUsageDescription": "Pith records your microphone so your voice is included in the meeting mix. It does not join the call as a bot.",
        "NSAudioCaptureUsageDescription": "Pith records your microphone so your voice is included in the meeting mix. It does not join the call as a bot.",
        "NSScreenCaptureUsageDescription": "Pith captures meeting sound from other apps (Zoom, Meet, Teams), not your screen or video."
      }
    },
    "win": {
      "target": [
        "nsis"
      ]
    }
  }
}
